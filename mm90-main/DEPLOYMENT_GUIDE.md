# موقع ديزل كفر شكر 🛍️

موقع تجارة إلكترونية لبيع الملابس الرجالية والشبابية

## 📋 المتطلبات

- **الاستضافة**: استضافة ثابتة (Static Hosting) فقط
- **قاعدة البيانات**: Firebase (مجاني)
- **الدفع**: الدفع عند الاستلام فقط

## 🚀 الملفات المطلوبة للرفع على الاستضافة

```
📁 الملفات الأساسية:
├── index.html
├── admin.html (لوحة التحكم)
├── css/
│   └── style.css
├── js/
│   ├── main.js
│   ├── products.js
│   └── admin_new.js
├── images/
│   └── (كل الصور)
├── firebase-messaging-sw.js
├── robots.txt
├── sitemap.xml
└── .nojekyll (للـ GitHub Pages فقط)
```

## ❌ الملفات اللي مش محتاجة على الاستضافة

```
📁 ملفات محلية فقط:
├── node_modules/ (مش محتاجة)
├── package.json (للتطوير المحلي فقط)
├── package-lock.json
├── server.js (سيرفر محلي للتطوير)
├── sync_orders.js (يشتغل على جهازك)
├── run_sync.bat
├── serviceAccountKey.json (سري - لا ترفعه!)
└── .git/
```

## 📦 خطوات الرفع على الاستضافة

### 1️⃣ تحضير الملفات
- احذف المجلدات: `node_modules`, `.git`
- احذف الملفات: `server.js`, `sync_orders.js`, `serviceAccountKey.json`
- اضغط الباقي في ملف ZIP

### 2️⃣ رفع الملفات
- افتح لوحة تحكم الاستضافة (cPanel أو File Manager)
- ارفع الملفات في مجلد `public_html` أو `www`
- تأكد من وجود `index.html` في المجلد الرئيسي

### 3️⃣ ربط الدومين
- اربط الدومين المجاني من لوحة التحكم
- انتظر 24-48 ساعة للتفعيل

## 🔧 الصيانة والتحديثات

### إضافة منتجات جديدة
1. افتح `https://yoursite.com/admin.html`
2. سجل دخول بحساب الأدمن
3. أضف/عدل/احذف المنتجات

### تحميل تقارير الطلبات (على جهازك)
```bash
# في مجلد المشروع على جهازك
node sync_orders.js
```
سيتم إنشاء ملف Excel: `تقرير_المبيعات_اليومي.xlsx`

## 🔐 الأمان

- ✅ Firebase Rules محمية
- ✅ لوحة الأدمن محمية بـ Firebase Auth
- ⚠️ **لا ترفع** ملف `serviceAccountKey.json` على الاستضافة أبداً!

## 📞 الدعم الفني

للمساعدة: واتساب +20 10 66992165

---

**ملاحظة**: الموقع لا يحتاج Node.js على السيرفر، كل شيء يعمل من المتصفح مباشرة! ✨
